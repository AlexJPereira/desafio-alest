export const server = {
    port: 80
}

// essas informacoes nao podem ser publicas
export const firebaseAdmin = {
    projectId: "id do projeto",
    privateKey: "chave privada do peojeto",
    clientEmail: "email do cliente"
}

export default { server, firebaseAdmin }